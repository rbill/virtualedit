package bank ; public class Account {
	int ownedAmount
	;
	public void addAmount ( int amount ) {
		ownedAmount = ( ownedAmount + amount );
	}
	public void banktransfer ( Account toAccount , int amount ) {
		toAccount.addAmount(( amount - 1 ));
		this.addAmount(( 0 - amount ));
	}
}